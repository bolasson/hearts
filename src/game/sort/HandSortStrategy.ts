/**
 * @deprecated The strategy-pattern interface was replaced by the dynamic
 * `sortHandWith(hand, config)` function in `./index.ts`. This file is kept
 * only because the mounted filesystem can't be cleaned from the sandbox;
 * it's no longer imported by anything.
 *
 * Safe to delete from a regular terminal:
 *   del src\\game\\sort\\HandSortStrategy.ts
 */
export {};
